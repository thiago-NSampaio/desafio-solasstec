services:
  postgres:
    container_name: solasstec-pg
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: solasstec
      PGDATA: /data/postgres
    volumes:
      - ./data/pg:/data/postgres